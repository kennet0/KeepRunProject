<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mybatis.example.boardMapper">


	<select id="lastWork" resultType="WorkProcessVO">
		<![CDATA[SELECT * FROM w_data where deviceId=#{deviceId} ORDER BY bno DESC LIMIT 0,1]]> 
	</select>

 	<select id="wList" resultType="WorkProcessVO">
		<![CDATA[SELECT * FROM w_data where deviceId=#{deviceId} ORDER BY bno DESC ]]> 
	</select>
	
	<select id="weekKcal" resultType="Integer">
		<![CDATA[SELECT kcal FROM w_data WHERE YEARWEEK(workDate) = YEARWEEK(#{workDate}) ORDER BY bno desc]]> 
	</select>
	
	<select id="dayDeviceData" resultType="DeviceDataVO">
		<![CDATA[SELECT * FROM a_data WHERE DATE(sendTime) = #{workDate} ORDER BY dno ]]> 
	</select>
	
	
	
	
	
   
</mapper>
